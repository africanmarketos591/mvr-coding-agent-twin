# Install as mvr/.gitignore — REQUIRED before first commit (see SECURITY.md).
# NEVER committed (personal data / local cache):
passport.json
state.json
.twin-nudge-*
# ALWAYS committed (the audit trail — do not add these here):
#   decision-log.json, build_spec.json, build-contract-history.jsonl,
#   user-brief.txt, semantic-review-request.json, semantic-review.json,
#   semantic-review-2.json, gate-events.jsonl, charters, mirrors
