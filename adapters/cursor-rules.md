# Cursor adapter — MVR Twin (place content in `.cursor/rules/mvr-twin.mdc` or `.cursorrules`)

Binding: read `mvr-coding-agent-twin/CLAUDE.md` in full and obey it. Summary of the six laws
for rule-length limits: (1) judge before code — internal committee, Build Charter before
feature building; (2) never interrogate — infer, then ship the MIRROR artifact for correction;
(3) call the kernel spine at pre-charter/pre-claim/pre-export, quote its outputs verbatim,
spine wins market claims; (4) founder claims weigh 0.30 until attested; (5) never fabricate —
every named market fact needs source+date or `UNKNOWN - not verified`; unverified facts never
justify redirects and never unblock claims (default-deny); (6) outside African high-context
calibration, downgrade to lens-only and say so.

Enforcement on Cursor: harness hooks are limited — REQUIRED: install the git pre-commit gate
(`hooks/pre_commit_claim_gate.py`) so claim artifacts cannot be committed without decision-log
authorization. Read `mvr/state.json` each turn; staleness >30d = authorization void.
