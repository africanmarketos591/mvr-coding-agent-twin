# Replication Receipts - MVR Coding Agent Twin

This file is public-safe evidence for the controlled beta. It is intentionally not a victory lap. The misses, near-passes, and limits are part of the record.

## Current Status

Package: see `VERSION`.

Release class: controlled beta.

Authority boundary: full Skeptic/spine behavior requires a controlled beta key. STANDARD/sandbox mode is not the full Twin.

Not yet claimed:

- independent human validation,
- signed commercial diligence,
- public full-authority release,
- global calibration outside African/high-context markets.

## What Has Been Verified

- The Twin can redirect unsafe market-facing builds before code.
- The Twin can avoid reckless-NO on a positive-control internal tool.
- The claim gate fails closed for unauthorized claim artifacts.
- The git pre-commit gate gives host-agnostic claim enforcement.
- Heartbeat counsel fails silent while authority gates fail closed.
- Preregistration hashes are canonical and verifiable.
- Duplicate preregistration headers fail closed.
- Ledger preregistration status is derived from append-only anchor updates.
- Ledger inflation is mechanically rejected.
- Manifest generation is Python-only, strict UTF-8, no BOM.
- Internal key-file parsing ignores human labels and accepts only explicit key fields.

## Rehearsal Runs

### Strict Blind Replication

Result: near-pass, not upgraded by narrative.

The strict run produced useful evidence but did not clear the hard threshold. The team kept it as a near-pass because the control prompts were MVR-shaped enough to compress the delta.

Why this matters: the product's evidence culture requires visible misses. A near-pass remains a near-pass.

### Looser Real-Founder Prompt Replication

Result: controlled developer preview / controlled beta preparation pass.

The looser run produced clear pre-code redirection:

- a risky credit/marketplace build was redirected to a safer, narrower wedge;
- a positive-control internal dashboard was allowed rather than blocked.

### Antigravity Rehearsal

Result: controlled beta rehearsal PASS.

Ledger status:

- hash-verified,
- decision-logged,
- audit-clean,
- anchor-pending.

Not yet fully preregistered until public git and Wayback anchors are recorded through `scripts/ledger_audit.py --add-anchor`.

## Defect Classes Found And Closed

- Hook matcher gap.
- Substring authorization bypass.
- BOM-tolerant decision-log handling.
- Stale version heading policy.
- Passport privacy leak risk.
- Host-without-hooks enforcement gap.
- Self-invalidating preregistration hash.
- Unsafe key-file label parsing.
- Duplicate preregistration header ambiguity.
- Ledger preregistration inflation risk.
- Manifest UTF-8 BOM/release-process risk.

Each class now has a tool, test, or binding rule intended to prevent recurrence.

## Remaining Human Gates

- Public git + Wayback anchors for rehearsal charters.
- Founder decision on TWIN-scoped/free-tier Skeptic access.
- D6 reviewer/signature role.
- Separated Round-3 human operator and scorer.
- First external cohort or accelerator run.

The next meaningful evidence should come from an outside user's charter, not another internal review.
